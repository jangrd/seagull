https://colorhunt.co/palette/1b3c53234c6a456882d2c1b6

CODE FIX:
 !! - cleanse whitespace in whole project
 !! - handle asserts with proper errors
	- make things that loop through elements use the windows index
	  so the tree is traversed only once
	- add success flag to SGL_VectorAdd because malloc can fail, but the return value is already used
	- remove 128 byte limit for SGL_Element.c -> fetchNextByDelim()
	- rearrange structs so they take up less memory
	- use memory checker to find leaks (i think there are a few so far)
	- make screen update only when a change is detected (global flag?, element flag?, window flag?)
	- cap fps to monitor refresh rate (default) or user specified value
	- do my own memory management for index
	  allocate a block of memory then manually place the index nodes inside
	  helps with cache hits
	  possibly, idk honestly

SECURITY FIX:
	- handle double free by calling dectructor twice (counter??? state checker???)
	  debug mode only???

FUTURE IDEAS:
	- add some kind of debug mode
	  browser-like devtools?
	  hot reload????
	- add tui to project script
	- make nice space-based text padding algorithm
	  make it keep output inside 80 columns
	  add colors to messages
